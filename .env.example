# 环境配置示例
# 复制此文件为 .env 并根据需要修改

# 部署环境：local（本地）、cloud（云服务器）、docker（容器）
# 如果不设置，系统会自动检测
# DEPLOY_ENV=local

# 监听地址
# 本地开发：127.0.0.1（只允许本机访问）
# 生产环境：0.0.0.0（允许所有IP访问）
# HOST=0.0.0.0

# 监听端口
# 本地开发：8090（避免与其他服务冲突）
# 云服务器：8000（配合 Nginx 反向代理）
# PORT=8090

# 调试模式
# DEBUG=false

# 示例配置

# === 本地开发 ===
# DEPLOY_ENV=local
# HOST=127.0.0.1
# PORT=8090
# DEBUG=true

# === 云服务器（使用 Nginx 反向代理）===
# DEPLOY_ENV=cloud
# HOST=127.0.0.1
# PORT=8000
# DEBUG=false

# === 云服务器（直接访问）===
# DEPLOY_ENV=cloud
# HOST=0.0.0.0
# PORT=8080
# DEBUG=false

# === Docker 容器 ===
# DEPLOY_ENV=docker
# HOST=0.0.0.0
# PORT=8000
# DEBUG=false
